import Axios from "../../../../axiosConfig/Axios";

//api to fetch dags of the specified version of a table
export const fetchDagsOfVerTable = (domainName, dataSetName, tableName, versionID) => {
    let response = require("../../.././../jsonFiles/dags.json");
    return response;
};
