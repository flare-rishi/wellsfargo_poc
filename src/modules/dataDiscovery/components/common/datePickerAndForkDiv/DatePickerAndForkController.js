import React from "react";
import DatePickerAndFork from "./DatePickerAndFork";

const DatePickerAndForkController = ({ type }) => {
  return <DatePickerAndFork type={type} />;
};

export default DatePickerAndForkController;
