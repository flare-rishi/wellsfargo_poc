import Axios from "../../../../axiosConfig/Axios";

//fetch all  tables  of a dataset

export const fetchAllTables = (domainName, dataSetName) => {
    let response = require("../../.././../jsonFiles/dataSetData.json");
    return response;
};
